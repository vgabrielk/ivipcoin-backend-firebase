class Task {
    constructor(id, task_title, task_status ) {
            this.id = id;
            this.task_title = task_title;
            this.task_status = task_status
    }
}

module.exports = Task;